# Uncomment the next line to define a global platform for your project
source 'https://cdn.cocoapods.org/'
platform :ios, '13.0'

target 'VideoPlayerView' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for VideoPlayerView
  pod 'MobileVLCKit'

  
  target 'VideoPlayerViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideoPlayerViewUITests' do
    # Pods for testing
  end

end
